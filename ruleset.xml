<?xml version="1.0"?>
<ruleset name="locomotive">
	<description>Sniffs for the coding standards of the locomotive plugin</description>

	<exclude-pattern>./vendor/*</exclude-pattern>
	<exclude-pattern>./tests/*</exclude-pattern>
	<exclude-pattern>./node_modules/*</exclude-pattern>

	<rule ref="WordPress">
		<exclude name="WordPress.VIP.RestrictedFunctions" />
		<exclude name="WordPress.VIP.RestrictedVariables" />
		<exclude name="WordPress.VIP.SuperGlobalInputUsage" />
		<exclude name="WordPress.VIP.PostsPerPage" />
		<exclude name="WordPress.VIP.AdminBarRemoval" />
		<exclude name="WordPress.VIP.SlowDBQuery" />
		<exclude name="WordPress.VIP.DirectDatabaseQuery" />
		<exclude name="WordPress.CSRF.NonceVerification" />
	</rule>
</ruleset>
