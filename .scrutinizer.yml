build:
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=processing-coverage'
                coverage:
                    file: 'processing-coverage'
                    format: 'php-clover'
application:
    project_setup:
        before:
            - ./bin/install-wp-tests.sh
